# Text Chunk Zero Rewrite Acceptance Checklist

Scope: replace the current text chunk editor implementation from scratch, keep only the native keyboard-anchored rail transport where useful, and restyle that rail to match the table/list selection rail.

| ID | Source instruction | Intended code area | Acceptance condition | Verification method | Status |
| --- | --- | --- | --- | --- | --- |
| TCZR-01 | User: "0-ról fogod írni... törölsz minden ehhez kapcsolódó fájlt és újratervezed" | `lib/src/notes/ui/text_chunk_canvas_editor.dart`, `text_chunk_layout_model.dart`, `text_chunk_text_editing.dart`, related tests | Old canvas/layout/paragraph-step implementation is removed, not patched; new text chunk code is split into focused files. | DONE: `git diff --name-status` shows the old files deleted; `rg` has no old production/test symbols. | DONE |
| TCZR-02 | User: "a paragraph stepet is el kell veszteni" | Text chunk paragraph/indent logic | Old paragraph-step reflow implementation is deleted. Any new indent behavior is a new implementation, not the old soft-wrap insertion model. | DONE: `rg "TextChunkStep|applyTextChunkParagraphStep|buildTextChunkLayout" lib test` returns no hits. | DONE |
| TCZR-03 | User: "blank szövegszerkesztőhöz képest... bekezdések... kijelölés az egész textchunkot érintheti" | New text chunk editor widget/controller | Editor is a plain native Flutter text editor surface with stock selection handles and no synthetic placeholder text. Selection can span the whole text chunk. | DONE: widget tests inspect `EditableText` text equality, whole-chunk selection, and absence of old rail/spacer artifacts. | DONE |
| TCZR-04 | User: "rail viselkedése jó" and earlier keyboard-sync requirement | `native_selection_rail_bridge.dart`, Android `NativeSelectionRailBridge.kt`, screen state | Rail remains keyboard-anchored through native IME insets; shown for selected text and for a collapsed cursor inside an existing tagged range; hidden while typing untagged text. | DONE: widget/native channel tests verify visible/hidden rail state; Android CI compile still pending under TCZR-10. | DONE |
| TCZR-05 | User: "ugyanazt a designt ami a táblázat chunk railben van" | Android native rail renderer | Native rail uses table/list rail visual contract: white/grey background, `#E5E7EB` separator/border, icon buttons, 64 px action row, optional 48 px tag row, scrollable rows, tag row collapse toggle. | DONE: Dart payload tests, Android source inspection, and GitHub Android build `27935197238` passed. | DONE |
| TCZR-06 | User: "ugyanaz a szín, ugyanaz az ikon... step in step out gomb... 3 stilisztikai gomb" | Android native rail renderer and action model | Actions use table/list-equivalent Material icons: tag, clear, prev, next, indent decrease/increase, rounded, opacity, border, and collapse/expand. | DONE: action keys/order and Kotlin icon mapping verified; GitHub Android build `27935197238` passed. | DONE |
| TCZR-07 | User: "ugyanaz a két soros scrollable mód, amiben a tag sort be lehet zárni" | Android native rail renderer | Action row and tag row are independently horizontally scrollable; tag row can be collapsed/expanded. Empty tag row still matches table rail empty-state behavior. | DONE: renderer source uses two horizontal scroll rows and collapse state; GitHub Android build `27935197238` passed. | DONE |
| TCZR-08 | User: "ha sok aláhúzás van, az tolja lejjebb a szöveg alatti contentet" | New text chunk underline layer | Secondary tag underlines are visual overlays that reserve vertical room below editable content without inserting hidden text. | DONE: widget test asserts native text is unchanged, overlay exists, and many secondary tags increase editable content height. | DONE |
| TCZR-09 | User: "több fájlból álljon, hogy csak a relevánsakat kelljen törölni" | New text chunk file layout | New implementation is modular, with separate files for editing surface, tag ranges, underline layout, rail state/actions, and text transforms. | DONE: `rg --files lib/src/notes/ui/text_chunk` shows five focused files. | DONE |
| TCZR-10 | Global AGENTS.md | Tests/analysis/CI | No completion claim unless all non-deferred requirements are `DONE`; run targeted tests, `flutter analyze`, full tests, commit, push, and manual GitHub Android build. | DONE: targeted tests, route tests, `flutter analyze`, full `flutter test`, commit/push, and GitHub Android build `27935197238` passed. | DONE |
