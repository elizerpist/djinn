Scope: implement `docs/superpowers/specs/2026-06-21-note-atom-search-design.md` from restored git-history spec `9ea3252`, replacing role/facet/definition-driven note search with atomized evidence, bounded cascade, and visible reasons.

References:
- `docs/superpowers/specs/2026-06-21-note-atom-search-design.md`
- Historical source before restoration: `git show 9ea3252:docs/superpowers/specs/2026-06-21-note-atom-search-design.md`

| ID | Source | Intended code area | Acceptance condition | Verification method | Status |
| --- | --- | --- | --- | --- | --- |
| NAS-01 | Spec "A chunk szerkesztesi egyseg, az atom keresesi egyseg" and acceptance 1 | `note_atom_indexer.dart`, `note_aware_local_retriever.dart` | Mixed text chunks are searched/displayed as relevant atoms; `bolognai` does not return unrelated respiratory text from the same chunk by default. | Unit test with mixed text chunk query `bolognai`; direct inspection of returned `SourceEvidence.text`. | DONE |
| NAS-02 | Spec "Evidence Atomok" | `source_evidence.dart`, `note_atom_indexer.dart` | Evidence atoms carry one of `text_sentence`, `list_item`, `table_cell`, `table_row`, `flowchart_node`, `flowchart_edge`; there is no `hidden_note_sentence`. | Atom indexer tests assert atom types for text/list/table/flowchart and hidden-note text maps to `text_sentence`. | DONE |
| NAS-03 | Spec "Minden talalat kap indokot" and acceptance 7 | `source_evidence.dart`, retriever reason assignment, chat citation mapping | Every displayed note atom has at least one reason chip/value such as `direct_query`, `note_scope`, `table_column`, `process_link`, or `external_direct`. | Retriever tests assert nonempty reasons on every returned note atom; chat bubble/widget test asserts reason chips render. | DONE |
| NAS-04 | Spec "Nincsenek chunk kategoriak" and "Jelenlegi Mukodesbol Kiszedendo" | `note_aware_local_retriever.dart`, graph expansion | Query words like `definicio`, `terapia`, `<`, and `=` are treated as literal/weak tokens, not search modes, facet filters, or definition expansion gates. | Tests for `legzesi elegtelenseg definicio` and `terapia` confirm no role/facet filter path; code inspection removes `_QueryScope` role/facet helpers from note search path. | DONE |
| NAS-05 | Spec "Kulso Jegyzetek Szabalya" and acceptances 2, 4, 5 | `note_aware_local_retriever.dart` | Primary note can cascade internally; external notes only contribute direct atoms and never trigger automatic deep cascade; one-word `sulyos` does not deep expand. | Multi-note retriever tests for `legzesi elegtelenseg`, `sulyos`, and `sulyos legzesi elegtelenseg`. | DONE |
| NAS-06 | Spec "Tablazat" and acceptance 6 | `note_atom_indexer.dart`, retriever matching/reasons | `table_cell.searchText` includes chunk title, row header, column header, and cell value, so an `O2` cell is retrievable by `sulyos legzesi elegtelenseg`. | Atom indexer and retriever tests assert the O2 cell is returned with `table_column` and `table_cell` reasons. | DONE |
| NAS-07 | Spec "Flowchart" and acceptances 3, 5 | `note_atom_indexer.dart`, bounded graph expander | Flowchart nodes/edges are atoms; primary-note process/branch links can add linked atoms with `flowchart_branch`/`process_link` reasons without pulling external oxygen notes. | Retriever test with flowchart branch -> oxygen table verifies internal linked atoms and no external oxygen-only result. | DONE |
| NAS-08 | Spec "Chat UI" and acceptance 8 | `chat_citation.dart`, `local_answer_service.dart`, `chat_bubble.dart`, preview UI | Chat citations expose atom type, reasons, source note/chunk labels, short text, full chunk reference where available, visible reason chips, full chunk preview, and a preview action to start a new scope from the atom text. | Widget tests for citation rows/chips, preview metadata, full chunk preview, and start-scope action; local answer service mapping tests. | DONE |
| NAS-09 | Project workflow | Docs, Ubuntu Flutter commands, GitHub Actions | Spec, plan, and checklist are present; targeted tests and analyze pass in Ubuntu/proot; branch is committed and pushed for GitHub Actions APK workflow. | `proot-distro login ubuntu -- ... flutter test ...`, `flutter analyze`, GitHub Actions status. | DONE |
