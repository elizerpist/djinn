| ID | Source instruction | Intended code area | Acceptance condition | Verification method | Status |
| --- | --- | --- | --- | --- | --- |
| PDFBOX-01 | User: "az appban jelenleg nem mukodik a pdf import, szurkek a pdf-ek, nem tudok betolteni az appba sajat tudastarat, ezt javitsd" | `lib/src/knowledge/ui/knowledge_base_screen.dart`, `lib/src/knowledge/data/pdf_import_service.dart` | PDF/PNG import works even when Android returns picked file bytes instead of a filesystem path; files are copied into app storage and listed in the knowledge base. | Widget/unit tests covering path import, bytes import, and pathless picker result; targeted test run. | DONE |
| PDFBOX-02 | Same import bug report | `lib/src/knowledge/ui/knowledge_base_screen.dart` | Import failures and skipped picker files are not silent; debug logs show picker result count, per-file source mode, copy result, repository insert, cancel, and error. | Widget test inspects `DebugConsole.messages`; code inspection. | DONE |
| PDFBOX-03 | User: "az osszes kert folyamathoz reszletes debug logokat is adj" | Knowledge import, PDF viewer, manual OCR/chunk box workflow | Logs exist for import, viewer open/load, manual box creation, box resize/move, type switch, extraction action, table operations, and flowchart operations. | Focused tests where practical plus direct code inspection for log tags. | PARTIAL |
| PDFBOX-04 | User: "minden boxnak legyen headerje, itt boxmuveletek lesznek" | `lib/src/knowledge/ui/manual_chunk_editor_screen.dart` and new focused widgets if needed | Every dropped extraction box has a persistent header with type label and actions. | Widget test opens manual chunk editor, creates a box, verifies header/action keys. | PARTIAL |
| PDFBOX-05 | User: "pl cella dobasa, oszlop dobasa" | Manual table extraction editor | Table extraction box supports row/column/cell manual controls and keeps a preview/draft of cell mapping. | Widget test drives table box controls and verifies draft cell state/logs. | PARTIAL |
| PDFBOX-06 | User: "flowchart box mini editor lesz, ad egy alap abrat ocr alapjan, de a user modosit" | Manual flowchart extraction editor, existing flowchart editor model/widgets | Flowchart extraction box shows PDF crop background plus editable node/edge overlay draft; user can edit labels and graph structure before save. | Widget test creates flowchart box, verifies node overlay, edit action, edge action, and draft serialization. | PARTIAL |
| PDFBOX-07 | User: "az alapabra azt jelenti, hogy boxokat raprinteli a flowchart boxjaira" | Flowchart extraction draft model | OCR/vision draft node boxes are stored with source PDF rects and rendered over the original PDF region. | Model/widget test with synthetic draft source rects. | NOT DONE |
