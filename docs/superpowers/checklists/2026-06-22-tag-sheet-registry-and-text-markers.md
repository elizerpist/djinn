Scope: implement the approved 2026-06-21 tag sheet redesign, add a reusable local tag registry, and apply the latest 2026-06-22 override that text chunk tag feedback is highlight background plus a selectable corner count badge, with no secondary underlines or underline-driven line-height expansion. The 2026-06-21 underline wording is superseded; badge rollout for non-text chunks is deferred.

References:
- `docs/superpowers/specs/2026-06-21-tag-sheet-redesign-design.md`
- `/data/data/com.termux/files/home/tag-sheet-code-preview/index.html`
- `/storage/emulated/0/Pictures/Screenshots/Screenshot_20260622-193459.png`

| ID | Source | Intended code area | Acceptance condition | Verification method | Status |
| --- | --- | --- | --- | --- | --- |
| TSRT-01 | User 2026-06-22: "kivenni az underlinet és az underline triggerelt sorköznagyobbodást" | `tagged_text_visual.dart`, `note_text_chunk_editor_screen.dart`, shared list/table underline call sites | Underline painter/helper/widget APIs are removed; text chunks no longer draw underline layers and no tag count changes text line height or strut height. | `rg` confirms no underline APIs in `lib`; `git diff --check` passes; GitHub Actions run 27977447969 passes analyze, Flutter tests, and APK build. | DONE |
| TSRT-02 | User 2026-06-22: "highlight háttér plusz a kétféle sarokbadge" | `tagged_text_visual.dart`, text chunk count marker painter/menu | First tag still colors text background; extra text tags are represented only by the selected count marker mode. | Count marker and tagged text visual tests pass in GitHub Actions run 27977447969. | DONE |
| TSRT-03 | 2026-06-21 spec: no user-facing type/category dropdown | `tag_manager_sheet.dart`, tests | The sheet does not render a type dropdown; new tags use internal normalized type `custom`. | Code inspection and tests assert `tag-manager-type` is absent; GitHub Actions run 27977447969 passes. | DONE |
| TSRT-04 | 2026-06-21 spec: one tag container, no active/saved split | `tag_manager_sheet.dart` | The sheet renders one filtered pill area; active tags are full opacity and inactive tags are reduced opacity. | Code inspection verifies old active/saved split is deleted; GitHub Actions run 27977447969 passes. | DONE |
| TSRT-05 | 2026-06-21 spec and prototype: full-color rail-style pills with edit/delete inside | `tag_manager_sheet.dart`, `note_tag_pills.dart` if shared | Tag pills use their resolved full color, white compact label text, inline pencil edit, inline x delete; no avatar-dot grey chips. | Code inspection verifies custom full-color pills; widget tests pass in GitHub Actions run 27977447969. | DONE |
| TSRT-06 | 2026-06-21 spec: fixed folder bar | `tag_manager_sheet.dart`, tag repository | Folder bar contains `Összes`, `Mappa nélkül`, `Új mappa`, and user folders; selecting filters tags; moving to `Mappa nélkül` clears folder. | Repository tests added; sheet code implements folder filters and drag targets; GitHub Actions run 27977447969 passes. | DONE |
| TSRT-07 | 2026-06-21 spec: fixed editor area above keyboard | `tag_manager_sheet.dart` | Header, folder bar, and editor remain fixed; only pill area scrolls; editor follows `viewInsets.bottom` without exceeding safe top. | Code inspection verifies fixed column regions and bottom viewInsets; GitHub Actions run 27977447969 passes. | DONE |
| TSRT-08 | 2026-06-21 spec: no save button, immediate auto-save | `tag_manager_sheet.dart`, all showTagManagerSheet call sites | Toggling, adding, editing, deleting, and moving tags immediately update the target; close does not commit a separate pending state. | Call sites pass `onChanged`; tests use close instead of save; GitHub Actions run 27977447969 passes. | DONE |
| TSRT-09 | 2026-06-21 spec: reusable local tag database | `note_document.dart`, `entities.dart`, new notes tag repository files, `main.dart` dependency wiring | Tags have stable registry id, label, color slot id, optional folder id, normalized default type `custom`, and timestamps; ObjectBox and memory implementations expose the same repository API. | Memory tests and ObjectBox entity/repository added; CI ObjectBox codegen passes in GitHub Actions run 27977447969. | DONE |
| TSRT-10 | 2026-06-21 spec: compatibility for embedded tags | `note_document.dart`, new tag repository | Existing embedded `NoteKnowledgeTag` JSON still reads; opening the sheet seeds missing registry rows; new assignments write id/colorSlotId/folderId plus fallback label/color for old readers. | Serialization and repository tests pass in GitHub Actions run 27977447969. | DONE |
| TSRT-11 | 2026-06-21 spec: duplicate visible labels reuse existing tag | tag repository and sheet add/update flow | Adding a tag whose normalized label already exists selects/updates the existing registry entry instead of creating a duplicate. | Memory repository duplicate-label tests pass in GitHub Actions run 27977447969. | DONE |
| TSRT-12 | 2026-06-21 spec: all note tag entry points use new sheet | `notes_screen.dart`, `note_editor_route.dart`, text/list/table/flowchart chunk editors | Global note, chunk, range text, list item, table cell/scope, and flowchart node/edge tag entry points all use the same sheet and repository. Badge UI remains text-chunk-only in this pass. | All current call sites are wired to `TagRepository`; GitHub Actions run 27977447969 passes. | DONE |
| TSRT-13 | Project workflow | GitHub Actions | `flutter analyze`, relevant Flutter tests, and debug APK workflow pass after push. | GitHub Actions Android native build run 27977447969 passed: https://github.com/elizerpist/djinn/actions/runs/27977447969 | DONE |

Implementation note: the 2026-06-21 spec's secondary underline rendering requirement is explicitly superseded for this implementation by the user's later 2026-06-22 instruction. The central tag model still supports multiple assigned tags; visual text feedback uses background plus count marker instead of underline lanes.
