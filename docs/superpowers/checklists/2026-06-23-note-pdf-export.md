# Note PDF Export Acceptance Checklist

Spec: `docs/superpowers/specs/2026-06-23-note-pdf-export-design.md`
Plan: `docs/superpowers/plans/2026-06-23-note-pdf-export.md`

| ID | Source Instruction | Intended Code Area | Acceptance Condition | Verification Method | Status |
| --- | --- | --- | --- | --- | --- |
| PDF-01 | User requested note dropdown `Export as PDF` | `lib/src/notes/ui/notes_screen.dart` | Single selected note menu contains `Export as PDF`; multi-select remains JSON export/share only | Widget test in `test/notes_screen_test.dart`; full `flutter test` | DONE |
| PDF-02 | User requested preview before actual save | `lib/src/notes/ui/note_pdf_preview_screen.dart` | Export opens fullscreen PDF preview, and back returns without saving | Preview screen widget test with injected viewer; route code inspection in `notes_screen.dart`; full `flutter test` | DONE |
| PDF-03 | User requested save to phone-selected folder | `lib/src/notes/pdf/note_pdf_export_service.dart` | Preview save uses platform save picker with PDF bytes and `.pdf` filename | Service test with save adapter; code inspection for `FilePicker.saveFile`; full `flutter test` | DONE |
| PDF-04 | User requested chunk order controls PDF structure | `lib/src/notes/pdf/note_pdf_document_builder.dart` | PDF sections are generated in `NoteDocument.blocks` order | Unit test verifies PDF bytes from ordered paragraph, table, and flowchart blocks; builder loop inspection | DONE |
| PDF-05 | User approved no tag system in PDF | `lib/src/notes/pdf/note_pdf_document_builder.dart` | PDF builder does not render note tags, chunk tags, text range tag visuals, search metadata, aliases, roles, or indexing state | Unit test fixture includes tags/search metadata and generated PDF text excludes tag metadata; code inspection | DONE |
| PDF-06 | User required flowchart render as in editor, not text fallback | `lib/src/notes/pdf/note_flowchart_pdf_layout.dart`, `lib/src/notes/pdf/note_pdf_document_builder.dart` | Flowchart chunks render as visual nodes, edges, arrowheads, labels, and shapes | Layout tests plus PDF byte generation test; full `flutter test` | DONE |
| PDF-07 | User asked what happens for wide/large flowcharts; later clarified PDF must stay portrait | `lib/src/notes/pdf/note_flowchart_pdf_layout.dart` | Large flowcharts choose portrait fit or portrait overview plus tiled detail pages without unreadable shrink; landscape export is not used | Unit tests for portrait/normal and overview+tiled strategy; builder code inspection | DONE |
| PDF-08 | Spec requires readable document sections | `lib/src/notes/pdf/note_pdf_document_builder.dart` | Heading, paragraph, list, table, and flowchart blocks are separated into clearly labeled sections | Unit test and PDF builder code inspection | DONE |
| PDF-09 | Spec requires save/share debug logging | `lib/src/notes/pdf/note_pdf_export_service.dart` | Logs include start, generated bytes, preview path, save/share lifecycle, and flowchart pagination without full note text | Service/preview tests inspect `DebugConsole.messages`; code inspection for build and flowchart logs | DONE |
| PDF-10 | Project workflow requires Ubuntu Flutter verification | tests/docs | Targeted tests, `flutter analyze`, and full `flutter test` run from Ubuntu/proot | Ubuntu/proot commands completed: targeted PDF/notes/editor tests passed, `flutter analyze` clean, full `flutter test` 419 passed / 8 skipped | DONE |
| PDF-11 | Code review found flowcharts appended after ordinary blocks | `lib/src/notes/pdf/note_pdf_document_builder.dart` | Mixed notes export/render blocks in exact `NoteDocument.blocks` order, including flowchart blocks between text blocks | Regression test inspects ordered debug render logs; targeted and full tests passed | DONE |
| PDF-12 | User required flowcharts render labels as in editor | `lib/src/notes/pdf/note_flowchart_pdf_layout.dart`, `lib/src/notes/pdf/note_pdf_document_builder.dart` | Flowchart edge labels have layout anchors and are painted on PDF pages | Layout regression test for label anchors plus code inspection; targeted and full tests passed | DONE |
| PDF-13 | PDF excludes tag/search metadata | `lib/src/notes/pdf/note_pdf_export_service.dart`, `lib/src/notes/pdf/note_pdf_document_builder.dart` | Metadata-only notes are rejected as non-exportable because excluded metadata cannot create blank PDFs | Service regression test with tags/search aliases only; targeted and full tests passed | DONE |
| PDF-14 | Editor supports checkbox and hierarchy list modes | `lib/src/notes/pdf/note_pdf_document_builder.dart` | Checkbox lists render checkbox markers; hierarchy lists render top-level numbers and child dashes | Unit test for PDF list marker helper; targeted and full tests passed | DONE |
| PDF-15 | User screenshot/request: flowchart visible on phone should fit A4; never force landscape | `lib/src/notes/pdf/note_flowchart_pdf_layout.dart`, `lib/src/notes/pdf/note_pdf_document_builder.dart` | Flowchart export uses portrait A4 pages only and no longer returns/uses landscape pages | Layout regression test; builder code inspection; targeted PDF tests passed | DONE |
| PDF-16 | User requested editor default zoom 1:1 physical sizing: “ami itt 2cm annak a pdf-en is 2cm” | `lib/src/notes/models/note_flowchart_geometry.dart`, `lib/src/notes/pdf/note_flowchart_pdf_layout.dart` | Flowchart node coordinates and node dimensions are converted from editor dp units to PDF points at a fixed physical scale, and export never scales above that 1:1 size | Layout regression test for editor-visible wide chart fitting one portrait page at scale 1.0; targeted PDF tests passed | DONE |
| PDF-17 | User requested flowchart not split into 3 pages when it fits phone/editor screen | `lib/src/notes/pdf/note_flowchart_pdf_layout.dart` | A wide but editor-visible flowchart stays on one portrait page; overview/tiled pages are used only for genuinely oversized portrait content | Layout regression test for formerly-landscape/wide chart; targeted PDF tests passed | DONE |
| PDF-18 | User requested PDF export in the note’s own dropdown | `lib/src/notes/ui/note_editor_route.dart` | The opened note editor menu contains `Export as PDF` and exports only the current note | Widget test with injected PDF service/opener; targeted editor route test passed | DONE |
| PDF-19 | User requested notes menu batch PDF export for selected notes | `lib/src/notes/ui/notes_screen.dart`, `lib/src/notes/pdf/note_pdf_export_service.dart` | Multi-select selection menu contains `Export as PDF` and preview generation receives exactly all selected notes | Widget/service regression tests; targeted notes screen test passed | DONE |
| PDF-20 | User requested multiple notes start on new pages | `lib/src/notes/pdf/note_pdf_document_builder.dart` | Batch PDF builds one document from selected notes and starts each note after the first on a fresh page | Builder/export service test via debug logs and PDF byte generation; targeted PDF tests passed | DONE |
