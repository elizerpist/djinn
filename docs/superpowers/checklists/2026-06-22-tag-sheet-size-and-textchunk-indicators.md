Scope: fix the tag sheet folder creation crash, make the tag sheet height content-adaptive, mirror table chunk global tag pills in text chunks, add a third text tag count marker mode that shows only the number, and soften the current dark count marker backgrounds by deriving them from tag color.

References:
- `/storage/emulated/0/Pictures/Screenshots/Screenshot_20260622-222138.png`
- `lib/src/notes/ui/note_table_editor_screen.dart` table chunk tag subheader pattern
- `docs/superpowers/checklists/2026-06-22-tag-sheet-registry-and-text-markers.md`

| ID | Source | Intended code area | Acceptance condition | Verification method | Status |
| --- | --- | --- | --- | --- | --- |
| TSTI-01 | User: "red error új tag mappa trigger esetén" | `tag_manager_sheet.dart`, tests | Tapping `Új mappa`, submitting a folder name, and returning to the sheet does not produce a Flutter framework error/assert and selects the new folder. | `proot-distro login ubuntu -- bash -lc 'cd /data/data/com.termux/files/home/djinn-knowledge-ocr-inspector && /home/flutteruser/flutter/bin/flutter test test/tag_manager_sheet_test.dart test/tagged_text_visual_test.dart test/note_text_chunk_editor_screen_test.dart'`; full `flutter test`. | DONE |
| TSTI-02 | User: "tag sheet mérete legyen adaptív... ha üres a container area, akkor nem magas" | `tag_manager_sheet.dart`, tests | The sheet no longer forces `height=maxHeight`; empty/small tag areas produce a compact sheet while still clamping to viewport/keyboard. | Targeted widget test verifies empty sheet height is below the previous forced max; full `flutter test`. | DONE |
| TSTI-03 | User: "táblázatban ... subheaderbe a pillek. a textchunk esetében is ezt akarom" | `note_text_chunk_editor_screen.dart`, tests | Text chunks render global chunk tags in a subheader pill row below the app bar, matching the table pattern, with delete support. | Targeted widget test verifies `note-text-chunk-tag-row` and `note-text-chunk-tag-pill-fontos`; full `flutter test`. | DONE |
| TSTI-04 | User: "textchunk dropdownba egy újabb tag chip jelölést: csak egy szám a jobb felső sarokban" | `tagged_text_visual.dart`, `note_text_chunk_editor_screen.dart`, tests | Overflow menu has a third marker mode; painter labels use only the tag count without `+` or fallback glyph. | Targeted widget/unit tests verify menu key, enum mode selection, and label `4`; full `flutter test`. | DONE |
| TSTI-05 | User: "jelenlegi kétféle tag indikátor chip pill opacityjét csökkentsd ... zavaró, hogy nagyon sötét fekete" | `tagged_text_visual.dart`, tests | Count marker background is derived from the first tag color with lower opacity, not hard black. | Unit test verifies marker run carries `colorValue` and fill color uses tag color at 0.72 alpha; full `flutter test`; `flutter analyze` passes after ObjectBox generation. | DONE |
